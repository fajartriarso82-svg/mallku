import Link from "next/link";
import { MapPin, Package, UserRound } from "lucide-react";
import { auth } from "@/lib/auth";
import { prisma } from "@/lib/db";
import { MarketplaceShell } from "@/components/mp/marketplace-shell";

export default async function AccountPage() { const session = await auth(); const userId = (session?.user as { id?: string } | undefined)?.id; const user = userId ? await prisma.user.findUnique({ where: { id: userId }, include: { buyerProfile: true, alamat: { orderBy: { isUtama: "desc" } } } }) : null; return <MarketplaceShell><main className="mx-auto max-w-[1000px] px-4 py-8 md:px-8"><div className="mb-8"><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#168bc3]">Akun pembeli</p><h1 className="mt-2 text-3xl font-extrabold text-[#26343f]">Halo, {user?.name || "Pembeli"}</h1><p className="mt-2 text-sm text-[#82919a]">Kelola profil, alamat pengiriman, dan pesanan Anda.</p></div><div className="grid gap-4 md:grid-cols-3"><div className="rounded-2xl border border-[#dce4e8] bg-white p-5 md:col-span-2"><div className="flex items-center gap-4"><span className="flex h-14 w-14 items-center justify-center rounded-2xl bg-[#e7f6fb] text-[#168bc3]"><UserRound className="h-6 w-6" /></span><div><h2 className="font-extrabold">{user?.buyerProfile?.nama || user?.name || "Pembeli"}</h2><p className="mt-1 text-sm text-[#82919a]">{user?.email}</p><p className="mt-1 text-xs text-[#82919a]">{user?.telepon || "Nomor telepon belum diatur"}</p></div></div></div><Link href="/pesanan" className="rounded-2xl border border-[#dce4e8] bg-white p-5 transition hover:border-[#168bc3]"><Package className="h-6 w-6 text-[#e53935]" /><h2 className="mt-4 font-extrabold">Pesanan saya</h2><p className="mt-1 text-xs text-[#82919a]">Lihat status dan riwayat pembelian</p></Link></div><div className="mt-6 rounded-2xl border border-[#dce4e8] bg-white p-5"><div className="flex items-center justify-between"><div><h2 className="font-extrabold">Alamat utama</h2><p className="mt-1 text-xs text-[#82919a]">Dipakai untuk menentukan pilihan lokal dan checkout.</p></div><Link href="/akun/alamat" className="text-xs font-bold text-[#168bc3]">Kelola alamat</Link></div>{user?.alamat[0] ? <div className="mt-4 rounded-xl bg-[#f7f9fa] p-4"><p className="text-xs font-bold text-[#26343f]">{user.alamat[0].label} · {user.alamat[0].namaPenerima}</p><p className="mt-1 text-sm leading-6 text-[#53616d]">{user.alamat[0].alamatLengkap}, {user.alamat[0].kecamatan}, {user.alamat[0].kabupatenKota}</p></div> : <div className="mt-4 rounded-xl border border-dashed border-[#cbd7dd] p-5 text-center"><MapPin className="mx-auto h-6 w-6 text-[#9aa8af]" /><p className="mt-2 text-sm font-semibold">Belum ada alamat tersimpan</p><Link href="/akun/alamat" className="mt-2 inline-block text-xs font-bold text-[#168bc3]">Tambah alamat utama</Link></div>}</div></main></MarketplaceShell>; }
